{
  "previewPort": 3000,
  "healthPath": "/",
  "startCommand": "docker compose -f docker-compose.base44.yml up -d",
  "secrets": [
    {
      "name": "VITE_BASE44_APP_ID",
      "description": "The Base44 app ID (from the Base44 Builder / project settings). Used by the frontend SDK to identify the app.",
      "requiredAtBoot": false
    },
    {
      "name": "VITE_BASE44_APP_BASE_URL",
      "description": "The Base44 backend URL for this app (e.g. https://my-app.base44.app). Found in the Base44 Builder.",
      "requiredAtBoot": false
    }
  ]
}
